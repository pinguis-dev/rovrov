# BE-005: Supabase Auth設定

## 基本情報
- **フェーズ**: 3 - 認証システム
- **優先度**: Critical
- **見積もり**: 3h
- **前提条件**: BE-001完了
- **タイプ**: Backend

## 実装内容

### 主要タスク
- [ ] マジックリンク認証有効化
- [ ] メールテンプレートカスタマイズ（日本語対応）
- [ ] リダイレクトURL設定（rovrov://auth/callback）
- [ ] JWT有効期限設定（access: 1h, refresh: 30d）
- [ ] auth.users作成時のprofilesレコード自動作成トリガー
- [ ] セッション管理設定

### 詳細設定
1. **マジックリンク認証**:
   - Supabaseダッシュボードでマジックリンク認証を有効化
   - カスタムSMTP設定（日本語メール対応）
   - リンク有効期限設定（デフォルト1時間）

2. **メールテンプレート**:
   - 日本語件名・本文設定
   - RovRovブランディング適用
   - モバイルアプリ用リダイレクトURL組み込み

3. **JWT設定**:
   - Access Token: 1時間有効
   - Refresh Token: 30日有効
   - 自動更新設定

4. **プロファイル自動作成**:
   ```sql
   CREATE OR REPLACE FUNCTION handle_new_user()
   RETURNS trigger AS $$
   BEGIN
     INSERT INTO public.profiles (id, username, display_name, created_at)
     VALUES (NEW.id, COALESCE(NEW.raw_user_meta_data->>'username', 'user_' || substr(NEW.id::text, 1, 8)), 
             COALESCE(NEW.raw_user_meta_data->>'display_name', 'New User'), NOW());
     RETURN NEW;
   END;
   $$ LANGUAGE plpgsql SECURITY DEFINER;
   
   CREATE TRIGGER on_auth_user_created
     AFTER INSERT ON auth.users
     FOR EACH ROW EXECUTE PROCEDURE handle_new_user();
   ```

## 参照ドキュメント
- `api-interface.md`: 1. 認証 (Authentication)
- `design.md`: 5.1 認証フロー

## テスト内容
- [ ] マジックリンク送信が正常に動作すること
- [ ] 日本語メールテンプレートが適用されていること
- [ ] ディープリンクリダイレクトが動作すること
- [ ] JWT有効期限が正しく設定されていること
- [ ] 新規ユーザー登録時にプロファイルレコードが自動作成されること
- [ ] トークンの自動更新が動作すること

## セキュリティ考慮事項
- [ ] SMTP認証情報の暗号化保存
- [ ] リダイレクトURL許可リストの適切な設定
- [ ] JWT署名キーの適切な管理
- [ ] セッション管理のセキュリティ設定
- [ ] メール配信レート制限の設定

## パフォーマンス考慮事項
- [ ] メール送信の非同期処理設定
- [ ] JWT検証の最適化
- [ ] セッション管理の効率化
- [ ] プロファイル作成トリガーの最適化

## 完了チェック
- [ ] マジックリンク認証が有効化されている
- [ ] 日本語メールテンプレートが設定されている
- [ ] リダイレクトURLが正しく設定されている
- [ ] JWT有効期限が適切に設定されている
- [ ] プロファイル自動作成トリガーが動作している
- [ ] 次フェーズ（FE-002）の前提条件が満たされている

## 追加設定
- [ ] メール配信制限の設定（スパム防止）
- [ ] 認証失敗時のレート制限
- [ ] セッション無効化処理の実装
- [ ] パスワードレス認証の追加セキュリティ設定

## 注意事項
- カスタムSMTPを使用する場合、事前にメール配信サービスの設定が必要
- リダイレクトURLはモバイルアプリのUniversal Links/App Linksと一致させる必要がある
- トリガー関数は権限昇格（SECURITY DEFINER）が必要
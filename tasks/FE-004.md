# FE-004: プロファイル編集画面実装

## 基本情報

- **フェーズ**: 4 - プロファイル管理
- **優先度**: High
- **見積もり**: 5h
- **前提条件**: FE-003完了
- **タイプ**: Frontend

## 実装内容

### 主要タスク

- [ ] ProfileEditScreen.tsx:
  - フォーム実装（react-hook-form使用）
  - 画像選択（expo-image-picker）
  - リアルタイムバリデーション
  - username一意性チェック（デバウンス付き）
- [ ] 画像アップロード処理:
  - 圧縮処理（expo-image-manipulator）
  - プログレス表示
  - エラーハンドリング
- [ ] 最適化:
  - フォーム入力のデバウンス
  - 画像のlazy loading

### 詳細実装

1. **ProfileEditScreen.tsx**:

   ```typescript
   import React, { useState, useEffect } from 'react';
   import { View, ScrollView, Alert, Text, TouchableOpacity, Image } from 'react-native';
   import { useForm, Controller } from 'react-hook-form';
   import * as ImagePicker from 'expo-image-picker';
   import * as ImageManipulator from 'expo-image-manipulator';
   import { useAuthStore } from '../store/authStore';
   import { profileApi } from '../services/profileApi';
   import { TextInput, Button, LoadingSpinner } from '../components/ui';
   import { useDebounce } from '../hooks/useDebounce';

   interface ProfileFormData {
     username: string;
     display_name: string;
     bio: string;
     location: string;
     website_url: string;
     avatar_url?: string;
     header_url?: string;
   }

   interface ProfileEditScreenProps {
     navigation: any;
     route: { params: { userId: string } };
   }

   export const ProfileEditScreen: React.FC<ProfileEditScreenProps> = ({ navigation, route }) => {
     const { user } = useAuthStore();
     const [isLoading, setIsLoading] = useState(false);
     const [isUploadingAvatar, setIsUploadingAvatar] = useState(false);
     const [isUploadingHeader, setIsUploadingHeader] = useState(false);
     const [usernameAvailable, setUsernameAvailable] = useState<boolean | null>(null);

     const {
       control,
       handleSubmit,
       watch,
       setValue,
       formState: { errors, isDirty }
     } = useForm<ProfileFormData>();

     const watchedUsername = watch('username');
     const debouncedUsername = useDebounce(watchedUsername, 500);

     // プロファイル取得
     useEffect(() => {
       if (user?.id) {
         loadProfile();
       }
     }, [user?.id]);

     // Username一意性チェック
     useEffect(() => {
       if (debouncedUsername && debouncedUsername.length >= 3) {
         checkUsernameAvailability(debouncedUsername);
       }
     }, [debouncedUsername]);

     const loadProfile = async () => {
       try {
         const profile = await profileApi.getProfile(user!.id);
         setValue('username', profile.username);
         setValue('display_name', profile.display_name || '');
         setValue('bio', profile.bio || '');
         setValue('location', profile.location || '');
         setValue('website_url', profile.website_url || '');
         setValue('avatar_url', profile.avatar_url || '');
         setValue('header_url', profile.header_url || '');
       } catch (error) {
         Alert.alert('エラー', 'プロファイルの読み込みに失敗しました');
       }
     };

     const checkUsernameAvailability = async (username: string) => {
       try {
         const isAvailable = await profileApi.checkUsernameAvailability(username);
         setUsernameAvailable(isAvailable);
       } catch (error) {
         setUsernameAvailable(null);
       }
     };

     const pickImage = async (type: 'avatar' | 'header') => {
       const { status } = await ImagePicker.requestMediaLibraryPermissionsAsync();
       if (status !== 'granted') {
         Alert.alert('権限エラー', '写真ライブラリへのアクセス権限が必要です');
         return;
       }

       const result = await ImagePicker.launchImageLibraryAsync({
         mediaTypes: ImagePicker.MediaTypeOptions.Images,
         allowsEditing: true,
         aspect: type === 'avatar' ? [1, 1] : [16, 9],
         quality: 0.8,
       });

       if (!result.canceled && result.assets[0]) {
         const asset = result.assets[0];
         await uploadImage(asset.uri, type);
       }
     };

     const uploadImage = async (uri: string, type: 'avatar' | 'header') => {
       const setLoading = type === 'avatar' ? setIsUploadingAvatar : setIsUploadingHeader;
       setLoading(true);

       try {
         // 画像圧縮
         const manipResult = await ImageManipulator.manipulateAsync(
           uri,
           [
             {
               resize: {
                 width: type === 'avatar' ? 300 : 800,
                 height: type === 'avatar' ? 300 : 450,
               }
             }
           ],
           {
             compress: 0.8,
             format: ImageManipulator.SaveFormat.JPEG,
           }
         );

         // Storage アップロード
         const uploadResult = await profileApi.uploadProfileImage(manipResult.uri, type);

         // フォームにURL設定
         setValue(type === 'avatar' ? 'avatar_url' : 'header_url', uploadResult.url, { shouldDirty: true });

         Alert.alert('成功', '画像をアップロードしました');
       } catch (error) {
         console.error('Image upload error:', error);
         Alert.alert('エラー', '画像のアップロードに失敗しました');
       } finally {
         setLoading(false);
       }
     };

     const onSubmit = async (data: ProfileFormData) => {
       if (!user?.id) return;

       setIsLoading(true);
       try {
         await profileApi.updateProfile(user.id, data);
         Alert.alert('成功', 'プロファイルを更新しました', [
           { text: 'OK', onPress: () => navigation.goBack() }
         ]);
       } catch (error: any) {
         const errorMessage = error.response?.data?.error?.message || '更新に失敗しました';
         Alert.alert('エラー', errorMessage);
       } finally {
         setIsLoading(false);
       }
     };

     const validateUsername = (value: string) => {
       if (value.length < 3) return '3文字以上で入力してください';
       if (value.length > 30) return '30文字以内で入力してください';
       if (!/^[a-zA-Z0-9_-]+$/.test(value)) return '英数字、アンダースコア、ハイフンのみ使用できます';
       if (usernameAvailable === false) return 'このユーザー名は既に使用されています';
       return true;
     };

     return (
       <ScrollView className="flex-1 bg-white">
         <View className="p-4">
           {/* ヘッダー画像 */}
           <TouchableOpacity
             className="h-48 bg-gray-200 rounded-lg mb-4 items-center justify-center"
             onPress={() => pickImage('header')}
           >
             {watch('header_url') ? (
               <Image source={{ uri: watch('header_url') }} className="w-full h-full rounded-lg" />
             ) : (
               <Text className="text-gray-500">ヘッダー画像を選択</Text>
             )}
             {isUploadingHeader && (
               <View className="absolute inset-0 bg-black/50 items-center justify-center rounded-lg">
                 <LoadingSpinner color="white" />
               </View>
             )}
           </TouchableOpacity>

           {/* アバター画像 */}
           <View className="items-center mb-6">
             <TouchableOpacity
               className="w-24 h-24 bg-gray-200 rounded-full items-center justify-center"
               onPress={() => pickImage('avatar')}
             >
               {watch('avatar_url') ? (
                 <Image source={{ uri: watch('avatar_url') }} className="w-24 h-24 rounded-full" />
               ) : (
                 <Text className="text-gray-500 text-xs">アバター</Text>
               )}
               {isUploadingAvatar && (
                 <View className="absolute inset-0 bg-black/50 items-center justify-center rounded-full">
                   <LoadingSpinner color="white" size="small" />
                 </View>
               )}
             </TouchableOpacity>
           </View>

           {/* フォーム */}
           <View className="space-y-4">
             <Controller
               control={control}
               name="username"
               rules={{ validate: validateUsername }}
               render={({ field: { onChange, value } }) => (
                 <View>
                   <TextInput
                     label="ユーザー名"
                     value={value}
                     onChangeText={onChange}
                     placeholder="username"
                     autoCapitalize="none"
                     error={errors.username?.message}
                   />
                   {usernameAvailable === true && (
                     <Text className="text-green-600 text-sm mt-1">✓ 使用可能です</Text>
                   )}
                   {usernameAvailable === false && (
                     <Text className="text-red-600 text-sm mt-1">✗ 既に使用されています</Text>
                   )}
                 </View>
               )}
             />

             <Controller
               control={control}
               name="display_name"
               rules={{
                 maxLength: { value: 50, message: '50文字以内で入力してください' }
               }}
               render={({ field: { onChange, value } }) => (
                 <TextInput
                   label="表示名"
                   value={value}
                   onChangeText={onChange}
                   placeholder="表示名"
                   error={errors.display_name?.message}
                 />
               )}
             />

             <Controller
               control={control}
               name="bio"
               rules={{
                 maxLength: { value: 300, message: '300文字以内で入力してください' }
               }}
               render={({ field: { onChange, value } }) => (
                 <TextInput
                   label="自己紹介"
                   value={value}
                   onChangeText={onChange}
                   placeholder="自己紹介を入力..."
                   multiline
                   numberOfLines={4}
                   error={errors.bio?.message}
                 />
               )}
             />

             <Controller
               control={control}
               name="location"
               render={({ field: { onChange, value } }) => (
                 <TextInput
                   label="所在地"
                   value={value}
                   onChangeText={onChange}
                   placeholder="東京, 日本"
                 />
               )}
             />

             <Controller
               control={control}
               name="website_url"
               rules={{
                 pattern: {
                   value: /^https?:\/\/.+/,
                   message: '有効なURLを入力してください'
                 }
               }}
               render={({ field: { onChange, value } }) => (
                 <TextInput
                   label="ウェブサイト"
                   value={value}
                   onChangeText={onChange}
                   placeholder="https://example.com"
                   keyboardType="url"
                   autoCapitalize="none"
                   error={errors.website_url?.message}
                 />
               )}
             />
           </View>

           {/* 保存ボタン */}
           <Button
             title="保存"
             onPress={handleSubmit(onSubmit)}
             loading={isLoading}
             disabled={!isDirty || isLoading}
             className="mt-6"
           />
         </View>
       </ScrollView>
     );
   };
   ```

2. **profileApi.ts**:

   ```typescript
   // services/profileApi.ts
   import { supabase } from './supabase';
   import { ProfileFormData } from '../types/profile';

   export const profileApi = {
     async getProfile(userId: string) {
       const { data, error } = await supabase
         .from('profiles')
         .select('*')
         .eq('id', userId)
         .single();

       if (error) throw error;
       return data;
     },

     async updateProfile(userId: string, profileData: ProfileFormData) {
       const { data, error } = await supabase
         .from('profiles')
         .update(profileData)
         .eq('id', userId)
         .select()
         .single();

       if (error) throw error;
       return data;
     },

     async checkUsernameAvailability(username: string): Promise<boolean> {
       const { data, error } = await supabase
         .from('profiles')
         .select('id')
         .eq('username', username)
         .single();

       return !data; // データが存在しない = 使用可能
     },

     async uploadProfileImage(uri: string, type: 'avatar' | 'header') {
       const fileExt = uri.split('.').pop();
       const fileName = `${Date.now()}.${fileExt}`;
       const filePath = `${type}s/${fileName}`;

       const response = await fetch(uri);
       const blob = await response.blob();

       const { data, error } = await supabase.storage
         .from(type === 'avatar' ? 'avatars' : 'headers')
         .upload(filePath, blob, {
           contentType: blob.type,
           cacheControl: '3600',
         });

       if (error) throw error;

       const {
         data: { publicUrl },
       } = supabase.storage.from(type === 'avatar' ? 'avatars' : 'headers').getPublicUrl(data.path);

       return { url: publicUrl };
     },
   };
   ```

3. **useDebounce Hook**:

   ```typescript
   // hooks/useDebounce.ts
   import { useState, useEffect } from 'react';

   export function useDebounce<T>(value: T, delay: number): T {
     const [debouncedValue, setDebouncedValue] = useState<T>(value);

     useEffect(() => {
       const handler = setTimeout(() => {
         setDebouncedValue(value);
       }, delay);

       return () => {
         clearTimeout(handler);
       };
     }, [value, delay]);

     return debouncedValue;
   }
   ```

## 参照ドキュメント

- `requirements.md`: 要件1（プロファイル管理）
- `api-interface.md`: 2.2 プロファイル更新

## テスト内容

- [ ] フォームの入力・バリデーションが正常に動作すること
- [ ] 画像選択・アップロード機能が動作すること
- [ ] username一意性チェックが機能すること
- [ ] プロファイル更新処理が成功すること
- [ ] エラーハンドリングが適切に動作すること
- [ ] デバウンス機能が正しく動作すること

## セキュリティ考慮事項

- [ ] 画像ファイルの検証処理
- [ ] 入力値のサニタイゼーション
- [ ] アップロード権限の確認
- [ ] 機密情報の適切な処理
- [ ] CSRF攻撃への対策

## パフォーマンス考慮事項

- [ ] 画像圧縮処理の最適化
- [ ] デバウンス処理による無駄なAPI呼び出しの削減
- [ ] フォーム状態管理の最適化
- [ ] 画像のメモリ効率的な処理
- [ ] キャッシュ活用によるUX向上

## 完了チェック

- [ ] ProfileEditScreenが実装されている
- [ ] フォーム機能が完全に動作している
- [ ] 画像アップロード機能が動作している
- [ ] バリデーション処理が適切に実装されている
- [ ] デバウンス機能が実装されている
- [ ] 次フェーズ（BE-007）の前提条件が満たされている

## UI/UXデザイン要件

- [ ] 直感的なフォームレイアウト
- [ ] 適切な入力フィードバック
- [ ] ローディング状態の明確な表示
- [ ] エラーメッセージの分かりやすさ
- [ ] 画像プレビュー機能
- [ ] レスポンシブなデザイン

## 型定義

```typescript
// types/profile.ts
export interface Profile {
  id: string;
  username: string;
  display_name?: string;
  bio?: string;
  avatar_url?: string;
  header_url?: string;
  location?: string;
  website_url?: string;
  created_at: string;
  updated_at?: string;
  status: 'active' | 'suspended' | 'pending_deletion';
}

export interface ProfileFormData {
  username: string;
  display_name: string;
  bio: string;
  location: string;
  website_url: string;
  avatar_url?: string;
  header_url?: string;
}
```

## 注意事項

- 画像圧縮は端末の性能を考慮してサイズと品質を調整
- username変更はシステム全体に影響するため慎重に実装
- iOS/Androidでの画像選択の動作差異に注意
- react-hook-formのバリデーションとAPI側バリデーションの整合性を保つ

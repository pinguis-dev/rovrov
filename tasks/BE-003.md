# BE-003: RLSポリシー・権限設定実装

## 基本情報
- **フェーズ**: 2 - データベース構築
- **優先度**: Critical
- **見積もり**: 7h
- **前提条件**: BE-002完了
- **タイプ**: Backend

## 実装内容

### 主要RLSポリシー実装
- [ ] public_profiles ビュー作成（pending_deletionを除外、anonymousアクセス用）
- [ ] profiles RLSポリシー（SELECT: status別可視性制御、UPDATE: 本人のみ）
- [ ] posts RLSポリシー（visibility + status + owner.status 複合判定）
- [ ] media_files RLSポリシー（親投稿のRLSを継承 + visibility制御）
- [ ] follows/friends RLSポリシー（相互関係の整合性チェック）
- [ ] favorites/reposts/pinnings/post_tags RLSポリシー
- [ ] blocks RLSポリシー（INSERT/DELETE: blocker_id = auth.uid()）
- [ ] GRANT設定（anon/authenticated/サービスロール別）

### 詳細実装仕様

#### 1. profiles RLSポリシー
```sql
-- 参照権限: status別制御
CREATE POLICY "profiles_select_policy" ON profiles
  FOR SELECT USING (
    CASE status
      WHEN 'active' THEN true
      WHEN 'suspended' THEN auth.uid() = id
      WHEN 'pending_deletion' THEN false
    END
  );

-- 更新権限: 本人のみ
CREATE POLICY "profiles_update_policy" ON profiles
  FOR UPDATE USING (auth.uid() = id);
```

#### 2. posts RLSポリシー
```sql
-- 参照権限: 複合条件
CREATE POLICY "posts_select_policy" ON posts
  FOR SELECT USING (
    status IN ('published', 'temporary') AND
    deleted_at IS NULL AND
    EXISTS (
      SELECT 1 FROM profiles 
      WHERE id = posts.user_id AND status = 'active'
    ) AND
    CASE visibility
      WHEN 'public' THEN true
      WHEN 'friends' THEN (
        auth.uid() = user_id OR
        EXISTS (SELECT 1 FROM friends WHERE ...)
      )
      WHEN 'private' THEN auth.uid() = user_id
    END
  );
```

#### 3. media_files 継承ポリシー
- 親投稿の可視性を継承
- visibility優先度: private > friends > public > inherit

### GRANT権限設定
- [ ] **anon**: SELECT ON posts, media_files, pins, public_profiles
- [ ] **authenticated**: SELECT/INSERT/UPDATE/DELETE ON 適切なテーブル
- [ ] **サービスロール**: 管理APIのみで使用

## 参照ドキュメント
- `state-and-policies.md`: 3.5 実装パターン（RLSポリシー例）、2. 権限設計
- `data-model.md`: RLSポリシー実装、可視範囲と権限制御
- `design.md`: 管理者権限とRLS

## テスト内容

### RLSポリシーテスト
- [ ] **profiles**: 各status（active/suspended/pending_deletion）でのアクセステスト
- [ ] **posts**: 各visibility（public/friends/private）× 各ユーザー関係でのテスト
- [ ] **media_files**: 親投稿の権限継承テスト
- [ ] **friends/follows**: 相互関係による権限変化テスト
- [ ] **blocks**: ブロック関係による制限テスト

### 権限マトリクステスト
| ロール | テーブル | SELECT | INSERT | UPDATE | DELETE |
|--------|----------|---------|---------|---------|---------|
| anon | posts | ○ | × | × | × |
| authenticated | posts | RLS | RLS | RLS | RLS |
| service | posts | ALL | ALL | ALL | ALL |

### エッジケーステスト
- [ ] 削除済みユーザーの投稿へのアクセス
- [ ] 停止中ユーザーの友達申請
- [ ] ブロック関係での双方向制限
- [ ] 退会予定ユーザーのコンテンツ非表示

## セキュリティ考慮事項
- [ ] **情報漏洩防止**: pending_deletionユーザーの完全非表示
- [ ] **権限昇格防止**: サービスロール使用の最小化
- [ ] **データ分離**: ユーザー間のデータアクセス制御
- [ ] **匿名アクセス制限**: 必要最小限のpublic権限のみ付与
- [ ] **管理者権限分離**: RLSバイパスは管理操作のみ

### 重要なセキュリティ実装
1. **profiles.status制御**:
   - suspended: 本人のみ閲覧可能
   - pending_deletion: 404扱い（完全非表示）

2. **書き込み禁止ガード**:
   - status != 'active'での書き込み操作全面禁止
   - トリガーレベルでの二重チェック

3. **ブロック機能の即時反映**:
   - RLSポリシーでの双方向制限
   - リアルタイム更新対応

## パフォーマンス考慮事項
- [ ] **RLS最適化**: インデックス活用可能なポリシー設計
- [ ] **結合最小化**: EXISTS句の効率的な記述
- [ ] **キャッシュ戦略**: ユーザー関係の頻繁な参照最適化
- [ ] **部分インデックス**: RLSと連携した検索最適化

### パフォーマンス重要ポイント
1. **friends/follows結合最適化**:
   - 事前作成済みインデックスとの整合
   - EXISTS句での効率的なサブクエリ

2. **profiles.status条件最適化**:
   - 部分インデックス（WHERE status = 'active'）活用
   - ポリシー条件での早期フィルタリング

## 完了チェック
- [ ] 全テーブルでRLS有効化（ALTER TABLE ... ENABLE ROW LEVEL SECURITY）
- [ ] 全ポリシーが作成・適用されている
- [ ] GRANT権限が適切に設定されている
- [ ] 匿名・認証・管理者の3ロールすべてでテスト完了
- [ ] エラーなく次フェーズ（BE-004）に進める状態

## 実装上の注意事項
- ポリシーの適用順序に依存関係あり（profiles → posts → media_files）
- テスト時はROLE切り替えによる権限検証必須
- 本番適用前に開発環境での十分な検証が必要
- RLSポリシーの変更は既存アプリケーションに影響するため慎重に実施
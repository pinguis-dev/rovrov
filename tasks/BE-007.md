# BE-007: メディアアップロードAPI実装

## 基本情報

- **フェーズ**: 5 - 投稿機能
- **優先度**: Critical
- **見積もり**: 5h
- **前提条件**: BE-001完了
- **タイプ**: Backend

## 実装内容

### 主要タスク

- [ ] POST /api/v1/media/upload-url（画像用）:
  - ファイルサイズ検証（max 10MB）
  - **MIMEスニッフィングと拡張子偽装対策**
  - MIME type検証（image/jpeg, image/png, image/gif）
  - 署名付きURL生成（1時間有効）
  - media_filesレコード作成（status: 'uploading'）
- [ ] POST /api/v1/media/stream/direct-upload（動画用）:
  - Cloudflare Stream API統合
  - tus resumable upload対応
  - **Webhook署名検証（cf-signatureヘッダー）**
  - **イベントID去重（冪等ハンドラ）**
  - **リトライ戦略（指数バックオフ、最大5回）**
  - **失敗時DLQ（Dead Letter Queue）と可視化ダッシュボード**
- [ ] メディア処理パイプライン:
  - EXIF除去処理（サーバー側強制）
  - 画像最適化（AVIF/WebP変換）
  - サムネイル生成

### 詳細実装

1. **POST /api/v1/media/upload-url（画像用）**:

   ```typescript
   import { createClient } from '@supabase/supabase-js';
   import crypto from 'crypto';

   export async function POST(request: Request) {
     const authUser = await getAuthUser(request);
     if (!authUser) {
       return new Response(
         JSON.stringify({ error: { code: 'UNAUTHORIZED', message: 'Authentication required' } }),
         { status: 401, headers: { 'Content-Type': 'application/json' } },
       );
     }

     const { fileName, fileSize, mimeType } = await request.json();

     // ファイルサイズ検証
     const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB
     if (fileSize > MAX_FILE_SIZE) {
       return new Response(
         JSON.stringify({
           error: { code: 'FILE_TOO_LARGE', message: 'File size must be less than 10MB' },
         }),
         { status: 413, headers: { 'Content-Type': 'application/json' } },
       );
     }

     // MIME type検証
     const allowedMimeTypes = ['image/jpeg', 'image/png', 'image/gif', 'image/webp'];
     if (!allowedMimeTypes.includes(mimeType)) {
       return new Response(
         JSON.stringify({
           error: {
             code: 'INVALID_FILE_TYPE',
             message: 'Invalid file type. Only JPEG, PNG, GIF, and WebP are allowed',
           },
         }),
         { status: 400, headers: { 'Content-Type': 'application/json' } },
       );
     }

     // 拡張子偽装対策
     const validExtensions = {
       'image/jpeg': ['.jpg', '.jpeg'],
       'image/png': ['.png'],
       'image/gif': ['.gif'],
       'image/webp': ['.webp'],
     };

     const fileExtension = fileName.toLowerCase().substring(fileName.lastIndexOf('.'));
     if (!validExtensions[mimeType].includes(fileExtension)) {
       return new Response(
         JSON.stringify({
           error: {
             code: 'EXTENSION_MISMATCH',
             message: 'File extension does not match MIME type',
           },
         }),
         { status: 400, headers: { 'Content-Type': 'application/json' } },
       );
     }

     try {
       // ユニークなファイルパス生成
       const fileId = crypto.randomUUID();
       const sanitizedFileName = fileName.replace(/[^a-zA-Z0-9.-]/g, '');
       const filePath = `posts/${authUser.id}/${fileId}/${sanitizedFileName}`;

       // media_filesレコード作成
       const { data: mediaFile, error: dbError } = await supabase
         .from('media_files')
         .insert({
           id: fileId,
           user_id: authUser.id,
           file_name: sanitizedFileName,
           mime_type: mimeType,
           file_size: fileSize,
           storage_path: filePath,
           status: 'uploading',
           created_at: new Date().toISOString(),
         })
         .select()
         .single();

       if (dbError) throw dbError;

       // 署名付きURL生成（1時間有効）
       const { data: signedUrl, error: urlError } = await supabase.storage
         .from('posts')
         .createSignedUploadUrl(filePath, {
           expiresIn: 3600, // 1時間
           upsert: false,
         });

       if (urlError) throw urlError;

       return new Response(
         JSON.stringify({
           media_id: fileId,
           upload_url: signedUrl.signedUrl,
           expires_at: new Date(Date.now() + 3600 * 1000).toISOString(),
         }),
         {
           headers: { 'Content-Type': 'application/json' },
         },
       );
     } catch (error) {
       console.error('Media upload URL generation error:', error);
       return new Response(
         JSON.stringify({
           error: { code: 'UPLOAD_URL_FAILED', message: 'Failed to generate upload URL' },
         }),
         { status: 500, headers: { 'Content-Type': 'application/json' } },
       );
     }
   }
   ```

2. **POST /api/v1/media/stream/direct-upload（動画用）**:

   ```typescript
   import { CloudflareStreamAPI } from '../services/cloudflareStream';

   export async function POST(request: Request) {
     const authUser = await getAuthUser(request);
     if (!authUser) {
       return new Response(
         JSON.stringify({ error: { code: 'UNAUTHORIZED', message: 'Authentication required' } }),
         { status: 401, headers: { 'Content-Type': 'application/json' } },
       );
     }

     const { fileName, fileSize } = await request.json();

     // ファイルサイズ検証（動画: 100MB）
     const MAX_VIDEO_SIZE = 100 * 1024 * 1024; // 100MB
     if (fileSize > MAX_VIDEO_SIZE) {
       return new Response(
         JSON.stringify({
           error: { code: 'FILE_TOO_LARGE', message: 'Video size must be less than 100MB' },
         }),
         { status: 413, headers: { 'Content-Type': 'application/json' } },
       );
     }

     try {
       const fileId = crypto.randomUUID();

       // Cloudflare Stream直接アップロード用URL作成
       const streamResponse = await CloudflareStreamAPI.createDirectUpload({
         maxDurationSeconds: 300, // 5分
         metadata: {
           userId: authUser.id,
           mediaId: fileId,
           fileName: fileName,
         },
       });

       // media_filesレコード作成
       const { data: mediaFile, error: dbError } = await supabase
         .from('media_files')
         .insert({
           id: fileId,
           user_id: authUser.id,
           file_name: fileName,
           mime_type: 'video/mp4', // Streamが自動変換
           file_size: fileSize,
           external_id: streamResponse.uid,
           storage_provider: 'cloudflare_stream',
           status: 'uploading',
           created_at: new Date().toISOString(),
         })
         .select()
         .single();

       if (dbError) throw dbError;

       return new Response(
         JSON.stringify({
           media_id: fileId,
           upload_url: streamResponse.uploadURL,
           stream_uid: streamResponse.uid,
           expires_at: new Date(Date.now() + 3600 * 1000).toISOString(),
         }),
         {
           headers: { 'Content-Type': 'application/json' },
         },
       );
     } catch (error) {
       console.error('Stream upload error:', error);
       return new Response(
         JSON.stringify({
           error: { code: 'STREAM_UPLOAD_FAILED', message: 'Failed to initialize video upload' },
         }),
         { status: 500, headers: { 'Content-Type': 'application/json' } },
       );
     }
   }
   ```

3. **Cloudflare Stream Webhook処理**:

   ```typescript
   // pages/api/webhooks/cloudflare-stream.ts
   import { CloudflareStreamAPI } from '../../../services/cloudflareStream';

   export async function POST(request: Request) {
     // Webhook署名検証
     const signature = request.headers.get('cf-signature');
     const body = await request.text();

     if (!CloudflareStreamAPI.verifyWebhookSignature(body, signature)) {
       return new Response('Invalid signature', { status: 401 });
     }

     const event = JSON.parse(body);

     // イベントID去重処理
     const eventId = event.id;
     const { data: existingEvent } = await supabase
       .from('webhook_events')
       .select('id')
       .eq('external_id', eventId)
       .single();

     if (existingEvent) {
       return new Response('Event already processed', { status: 200 });
     }

     // イベント記録
     await supabase.from('webhook_events').insert({
       external_id: eventId,
       provider: 'cloudflare_stream',
       event_type: event.eventType,
       processed_at: new Date().toISOString(),
     });

     try {
       await processStreamEvent(event);
       return new Response('OK', { status: 200 });
     } catch (error) {
       console.error('Stream webhook processing error:', error);
       // DLQ送信処理
       await sendToDLQ(event, error);
       return new Response('Processing failed', { status: 500 });
     }
   }

   async function processStreamEvent(event: any) {
     const { uid, eventType, metadata } = event;

     switch (eventType) {
       case 'video.upload.complete':
         await supabase
           .from('media_files')
           .update({
             status: 'ready',
             processed_at: new Date().toISOString(),
             metadata: { duration: event.duration, dimensions: event.dimensions },
           })
           .eq('external_id', uid);
         break;

       case 'video.upload.failed':
         await supabase
           .from('media_files')
           .update({
             status: 'failed',
             error_message: event.errorMessage,
             processed_at: new Date().toISOString(),
           })
           .eq('external_id', uid);
         break;
     }
   }
   ```

4. **画像処理パイプライン**:

   ```typescript
   // services/imageProcessor.ts
   import sharp from 'sharp';

   export class ImageProcessor {
     static async processUploadedImage(filePath: string) {
       try {
         // Storage上の画像を取得
         const { data, error } = await supabase.storage.from('posts').download(filePath);

         if (error) throw error;

         const buffer = Buffer.from(await data.arrayBuffer());

         // EXIF除去＋最適化処理
         const processedBuffer = await sharp(buffer)
           .rotate() // EXIF orientationに基づく回転
           .removeExif() // EXIF情報除去
           .jpeg({ quality: 85, progressive: true })
           .toBuffer();

         // 処理後画像を再アップロード
         await supabase.storage.from('posts').update(filePath, processedBuffer, {
           contentType: 'image/jpeg',
           cacheControl: '31536000', // 1年
         });

         // サムネイル生成（複数サイズ）
         const thumbnailSizes = [150, 300, 600];
         for (const size of thumbnailSizes) {
           const thumbnailBuffer = await sharp(processedBuffer)
             .resize(size, size, { fit: 'cover', position: 'center' })
             .jpeg({ quality: 80 })
             .toBuffer();

           const thumbnailPath = filePath.replace(/\.[^.]+$/, `_thumb_${size}.jpg`);
           await supabase.storage.from('posts').upload(thumbnailPath, thumbnailBuffer, {
             contentType: 'image/jpeg',
             cacheControl: '31536000',
           });
         }

         return { success: true };
       } catch (error) {
         console.error('Image processing error:', error);
         throw error;
       }
     }
   }
   ```

## 参照ドキュメント

- `api-interface.md`: 4. メディア (Media)
- `design.md`: 6.2 メディア処理

## テスト内容

- [ ] 画像アップロードURL生成が正常に動作すること
- [ ] ファイルサイズ・MIME type検証が機能すること
- [ ] 拡張子偽装対策が動作すること
- [ ] Cloudflare Stream統合が機能すること
- [ ] Webhook署名検証が正しく動作すること
- [ ] EXIF除去処理が実行されること
- [ ] サムネイル生成が正常に動作すること

## セキュリティ考慮事項

- [ ] アップロードファイルの詳細検証
- [ ] EXIF情報の完全除去
- [ ] 署名付きURLの適切な有効期限
- [ ] Webhook署名検証の実装
- [ ] ファイル名のサニタイゼーション
- [ ] アクセス権限の適切な制御
- [ ] 画像解凍爆弾攻撃への対策

## パフォーマンス考慮事項

- [ ] 画像処理の非同期実行
- [ ] サムネイル生成の最適化
- [ ] Storage操作の効率化
- [ ] リトライ機構の実装
- [ ] DLQ処理の効率化
- [ ] メモリ使用量の最適化

## 完了チェック

- [ ] 画像アップロードAPIが実装されている
- [ ] 動画アップロードAPIが実装されている
- [ ] Cloudflare Stream統合が完了している
- [ ] Webhook処理が実装されている
- [ ] 画像処理パイプラインが動作している
- [ ] 次フェーズ（BE-008）の前提条件が満たされている

## エラーコード定義

```typescript
export const MEDIA_ERROR_CODES = {
  FILE_TOO_LARGE: 'FILE_TOO_LARGE',
  INVALID_FILE_TYPE: 'INVALID_FILE_TYPE',
  EXTENSION_MISMATCH: 'EXTENSION_MISMATCH',
  UPLOAD_URL_FAILED: 'UPLOAD_URL_FAILED',
  STREAM_UPLOAD_FAILED: 'STREAM_UPLOAD_FAILED',
  PROCESSING_FAILED: 'PROCESSING_FAILED',
} as const;
```

## DLQ実装

```typescript
// services/dlq.ts
export const sendToDLQ = async (event: any, error: Error) => {
  await supabase.from('failed_webhook_events').insert({
    external_id: event.id,
    provider: 'cloudflare_stream',
    event_data: event,
    error_message: error.message,
    retry_count: 0,
    created_at: new Date().toISOString(),
  });
};
```

## 注意事項

- Cloudflare Stream APIキーは環境変数で管理し、本番環境では暗号化
- 画像処理は重い処理のため、適切なタイムアウト設定が必要
- Webhook処理は冪等性を保つため、イベントID去重が重要
- tus resumable uploadは大容量動画アップロードに対応するため実装

## 実装戦略

### 概要

本タスクでは、RovRov SNSアプリのメディアアップロード機能を実装します。画像・動画の安全なアップロード、処理、配信を実現するため、Supabase Edge Functions、Supabase Storage、Cloudflare Streamを統合した包括的なメディア処理パイプラインを構築します。セキュリティ、パフォーマンス、信頼性を重視した設計により、エンタープライズレベルのメディア管理システムを実現します。

### 技術アプローチ

#### アーキテクチャ選定

1. **Supabase Edge Functions**: Deno環境でのサーバーレス関数実行
   - 署名付きURL生成、権限検証、メタデータ管理
   - Cloudflare Stream APIとの統合レイヤー
2. **Supabase Storage**: 画像原本保存
   - 署名付きURLによる直接アップロード
   - サーバー側での後処理（EXIF除去、最適化）
3. **Cloudflare Stream**: 動画処理・配信
   - Direct Creator Upload（tus protocol）
   - 自動トランスコーディング、HLS配信
   - Webhook経由の処理状態同期

4. **Sharp.js**: 画像処理エンジン
   - EXIF除去、画像最適化
   - サムネイル生成（150px、300px、600px）
   - メモリ効率的な非同期処理

#### セキュリティ設計

- **多層防御戦略**:
  - MIMEタイプ検証（ホワイトリスト方式）
  - 拡張子偽装対策（MIME-拡張子整合性チェック）
  - ファイル名サニタイゼーション（パストラバーサル対策）
  - 実ファイル検証（Sharp.jsによる内容検証）
  - EXIF情報の完全除去（位置情報漏洩防止）

### ステップバイステップ実装

#### フェーズ1: 基盤セットアップ（1.5時間）

1. **Supabase Edge Functions環境構築**

   ```bash
   # プロジェクトルートで実行
   npx supabase init
   npx supabase functions new media-upload
   npx supabase functions new stream-upload
   npx supabase functions new cloudflare-webhook
   ```

2. **データベーステーブル準備**
   - media_filesテーブルの拡張（stream_uid、external_id列追加）
   - webhook_eventsテーブル作成（冪等性保証）
   - failed_webhook_eventsテーブル作成（DLQ）

3. **環境変数設定**
   ```env
   CLOUDFLARE_ACCOUNT_ID=xxx
   CLOUDFLARE_STREAM_API_TOKEN=xxx
   CLOUDFLARE_WEBHOOK_SECRET=xxx
   SUPABASE_SERVICE_ROLE_KEY=xxx
   ```

#### フェーズ2: 画像アップロードAPI実装（1.5時間）

1. **POST /api/v1/media/upload-url エンドポイント実装**
   - `supabase/functions/media-upload/index.ts`作成
   - 認証ミドルウェア統合
   - ファイルサイズ検証（10MB制限）
   - MIMEタイプ・拡張子検証ロジック
   - 署名付きURL生成（createSignedUploadUrl）
   - media_filesレコード作成（status: 'uploading'）

2. **画像処理パイプライン実装**
   - `supabase/functions/process-image/index.ts`作成
   - Storage Webhookトリガー設定
   - Sharp.js統合（EXIF除去、最適化）
   - サムネイル生成ロジック
   - 処理完了後のmedia_files更新

#### フェーズ3: 動画アップロードAPI実装（1時間）

1. **POST /api/v1/media/stream/direct-upload エンドポイント実装**
   - `supabase/functions/stream-upload/index.ts`作成
   - Cloudflare Stream API統合
   - Direct Creator Upload URL生成
   - media_filesレコード作成（storage_provider: 'cloudflare_stream'）

2. **Cloudflare Stream API サービス層**
   - `supabase/functions/lib/cloudflareStream.ts`作成
   - API認証、リクエスト署名
   - エラーハンドリング、リトライロジック

#### フェーズ4: Webhook処理実装（1時間）

1. **Cloudflare Stream Webhook受信エンドポイント**
   - `supabase/functions/cloudflare-webhook/index.ts`作成
   - 署名検証ロジック（cf-signatureヘッダー）
   - イベントID去重処理（webhook_eventsテーブル）
   - イベントタイプ別処理分岐

2. **DLQ（Dead Letter Queue）実装**
   - 処理失敗時のDLQ送信
   - リトライ戦略（指数バックオフ、最大5回）
   - 管理画面用ビュー作成（失敗イベント可視化）

### テスト戦略

#### ユニットテスト

- **検証ロジックテスト**:
  - MIMEタイプバリデーション
  - 拡張子偽装検出
  - ファイルサイズチェック
  - サニタイゼーション処理

#### 統合テスト

- **エンドツーエンドフロー**:
  - 画像アップロード完全フロー
  - 動画アップロード完全フロー
  - Webhook処理フロー
  - エラーリカバリーフロー

#### セキュリティテスト

- **攻撃シミュレーション**:
  - 画像解凍爆弾対策確認
  - パストラバーサル攻撃防御
  - XSS攻撃防御
  - CSRF攻撃防御

#### パフォーマンステスト

- **負荷テスト**:
  - 同時アップロード（100並列）
  - 大容量ファイル処理（10MB画像、100MB動画）
  - メモリリーク検出
  - CPUプロファイリング

### リスク考慮事項

#### 技術的リスク

1. **Sharp.jsメモリ管理**
   - リスク: 大量画像処理時のメモリ枯渇
   - 対策: ストリーム処理、バッファサイズ制限、並列度制御

2. **Cloudflare Stream API障害**
   - リスク: 外部サービス依存による可用性低下
   - 対策: グレースフルデグラデーション、代替ストレージフォールバック

3. **Webhook処理の信頼性**
   - リスク: イベント喪失、重複処理
   - 対策: 冪等性保証、DLQ実装、監視アラート

#### セキュリティリスク

1. **悪意のあるファイルアップロード**
   - リスク: マルウェア、スクリプト注入
   - 対策: 多層検証、サンドボックス処理、ウイルススキャン（将来）

2. **プライバシー漏洩**
   - リスク: EXIF位置情報、個人情報露出
   - 対策: サーバー側強制EXIF除去、アクセス制御

### 成功基準

#### 機能要件

- ✅ 画像アップロードAPI（10MB制限、JPEG/PNG/GIF/WebP対応）
- ✅ 動画アップロードAPI（100MB制限、Cloudflare Stream統合）
- ✅ EXIF除去処理（100%除去率）
- ✅ サムネイル生成（3サイズ、80%品質）
- ✅ Webhook処理（99.9%成功率）

#### 非機能要件

- ✅ レスポンス時間: 95パーセンタイル < 2秒
- ✅ 可用性: 99.9%以上
- ✅ エラー率: 1%未満
- ✅ メモリ使用量: 500MB未満/プロセス

#### セキュリティ要件

- ✅ OWASP Top 10対策実装
- ✅ ゼロトラストセキュリティモデル
- ✅ 監査ログ完備
- ✅ GDPR/個人情報保護法準拠

### 実装順序と依存関係

1. **前提条件確認**（BE-001完了確認）
   - Supabase初期設定
   - 認証システム稼働
   - RLSポリシー設定

2. **実装順序**:

   ```
   1. データベーススキーマ更新
   2. 環境変数設定
   3. 画像アップロードAPI実装
   4. 画像処理パイプライン実装
   5. 動画アップロードAPI実装
   6. Webhook受信エンドポイント実装
   7. DLQ実装
   8. 統合テスト実施
   9. セキュリティ監査
   10. パフォーマンスチューニング
   ```

3. **後続タスクへの影響**:
   - BE-008（投稿CRUD）: メディアID連携準備完了
   - FE-005（投稿画面）: アップロードAPI利用可能
   - BE-010（地図機能）: EXIF除去済み画像提供

### 実装チェックリスト

#### 初期セットアップ

- [ ] Supabase Functions初期化完了
- [ ] データベーススキーマ更新完了
- [ ] 環境変数設定完了
- [ ] 必要なnpmパッケージインストール完了

#### API実装

- [ ] 画像アップロードURL生成API実装
- [ ] 動画アップロードURL生成API実装
- [ ] Webhook受信エンドポイント実装
- [ ] エラーハンドリング実装

#### 処理パイプライン

- [ ] EXIF除去処理実装
- [ ] 画像最適化処理実装
- [ ] サムネイル生成処理実装
- [ ] DLQ処理実装

#### テスト

- [ ] ユニットテスト作成・実行
- [ ] 統合テスト作成・実行
- [ ] セキュリティテスト実施
- [ ] パフォーマンステスト実施

#### ドキュメント

- [ ] API仕様書更新
- [ ] 実装ガイド作成
- [ ] トラブルシューティングガイド作成
- [ ] 運用手順書作成

## テストケース

### 1. 画像アップロードAPI (POST /api/v1/media/upload-url)

#### 1.1 正常系テスト

##### TC-IMG-001: 正常な画像アップロードURL取得（JPEG）

- **テストID**: TC-IMG-001
- **テスト名**: 正常な画像アップロードURL取得（JPEG）
- **目的**: 有効なJPEG画像ファイルに対してアップロードURLが正常に生成されることを確認する
- **前提条件**:
  - ユーザーが正常にログイン認証済み
  - 有効なJWTトークンを保持
- **テスト手順**:
  1. POST /api/v1/media/upload-url に以下のリクエストを送信
     ```json
     {
       "fileName": "test_image.jpg",
       "mimeType": "image/jpeg",
       "fileSize": 1048576
     }
     ```
  2. レスポンスを確認
- **期待結果**:
  - HTTPステータス: 200 OK
  - レスポンス形式:
    ```json
    {
      "media_id": "uuid形式のメディアID",
      "upload_url": "署名付きSupabase Storage URL",
      "expires_at": "1時間後のISO 8601形式日時"
    }
    ```
  - media_filesテーブルにstatus='uploading'でレコード作成される
- **優先度**: Critical
- **テストタイプ**: 機能テスト

##### TC-IMG-002: 正常な画像アップロードURL取得（PNG）

- **テストID**: TC-IMG-002
- **テスト名**: 正常な画像アップロードURL取得（PNG）
- **目的**: 有効なPNG画像ファイルに対してアップロードURLが正常に生成されることを確認する
- **前提条件**: ユーザーが正常にログイン認証済み
- **テスト手順**:
  1. POST /api/v1/media/upload-url に以下のリクエストを送信
     ```json
     {
       "fileName": "test_image.png",
       "mimeType": "image/png",
       "fileSize": 2097152
     }
     ```
- **期待結果**: HTTPステータス200 OK、有効な署名付きURLとmedia_idが返される
- **優先度**: Critical
- **テストタイプ**: 機能テスト

##### TC-IMG-003: 正常な画像アップロードURL取得（GIF）

- **テストID**: TC-IMG-003
- **テスト名**: 正常な画像アップロードURL取得（GIF）
- **目的**: 有効なGIF画像ファイルに対してアップロードURLが正常に生成されることを確認する
- **前提条件**: ユーザーが正常にログイン認証済み
- **テスト手順**:
  1. POST /api/v1/media/upload-url に以下のリクエストを送信
     ```json
     {
       "fileName": "test_animation.gif",
       "mimeType": "image/gif",
       "fileSize": 5242880
     }
     ```
- **期待結果**: HTTPステータス200 OK、有効な署名付きURLとmedia_idが返される
- **優先度**: High
- **テストタイプ**: 機能テスト

#### 1.2 異常系テスト

##### TC-IMG-004: 未認証ユーザーによるアクセス

- **テストID**: TC-IMG-004
- **テスト名**: 未認証ユーザーによるアクセス
- **目的**: 認証トークンなしでのリクエストが適切に拒否されることを確認する
- **前提条件**: 認証トークンなし
- **テスト手順**:
  1. Authorizationヘッダーなしでリクエストを送信
- **期待結果**:
  - HTTPステータス: 401 Unauthorized
  - エラーレスポンス:
    ```json
    {
      "error": {
        "code": "UNAUTHORIZED",
        "message": "Authentication required"
      }
    }
    ```
- **優先度**: Critical
- **テストタイプ**: セキュリティテスト

##### TC-IMG-005: ファイルサイズ超過

- **テストID**: TC-IMG-005
- **テスト名**: ファイルサイズ超過エラー
- **目的**: 10MBを超えるファイルサイズでリクエストした際のエラー処理を確認する
- **前提条件**: ユーザーが正常にログイン認証済み
- **テスト手順**:
  1. POST /api/v1/media/upload-url に以下のリクエストを送信
     ```json
     {
       "fileName": "large_image.jpg",
       "mimeType": "image/jpeg",
       "fileSize": 11534336
     }
     ```
- **期待結果**:
  - HTTPステータス: 413 Payload Too Large
  - エラーレスポンス:
    ```json
    {
      "error": {
        "code": "FILE_TOO_LARGE",
        "message": "File size must be less than 10MB"
      }
    }
    ```
- **優先度**: High
- **テストタイプ**: エラーハンドリング

##### TC-IMG-006: 無効なMIMEタイプ

- **テストID**: TC-IMG-006
- **テスト名**: 無効なMIMEタイプエラー
- **目的**: サポートされていないMIMEタイプでのリクエストが適切に拒否されることを確認する
- **前提条件**: ユーザーが正常にログイン認証済み
- **テスト手順**:
  1. POST /api/v1/media/upload-url に以下のリクエストを送信
     ```json
     {
       "fileName": "document.pdf",
       "mimeType": "application/pdf",
       "fileSize": 1048576
     }
     ```
- **期待結果**:
  - HTTPステータス: 400 Bad Request
  - エラーレスポンス:
    ```json
    {
      "error": {
        "code": "INVALID_FILE_TYPE",
        "message": "Invalid file type. Only JPEG, PNG, GIF, and WebP are allowed"
      }
    }
    ```
- **優先度**: High
- **テストタイプ**: バリデーション

##### TC-IMG-007: 拡張子偽装対策

- **テストID**: TC-IMG-007
- **テスト名**: 拡張子偽装対策テスト
- **目的**: MIMEタイプと拡張子の不一致が検出され適切に拒否されることを確認する
- **前提条件**: ユーザーが正常にログイン認証済み
- **テスト手順**:
  1. POST /api/v1/media/upload-url に以下のリクエストを送信（.jpg拡張子だがMIMEタイプがPNG）
     ```json
     {
       "fileName": "fake_image.jpg",
       "mimeType": "image/png",
       "fileSize": 1048576
     }
     ```
- **期待結果**:
  - HTTPステータス: 400 Bad Request
  - エラーレスポンス:
    ```json
    {
      "error": {
        "code": "EXTENSION_MISMATCH",
        "message": "File extension does not match MIME type"
      }
    }
    ```
- **優先度**: Critical
- **テストタイプ**: セキュリティテスト

### 2. 動画アップロードAPI (POST /api/v1/media/stream/direct-upload)

#### 2.1 正常系テスト

##### TC-VID-001: 正常な動画アップロードURL取得（MP4）

- **テストID**: TC-VID-001
- **テスト名**: 正常な動画アップロードURL取得（MP4）
- **目的**: 有効なMP4動画ファイルに対してCloudflare StreamアップロードURLが正常に生成されることを確認する
- **前提条件**:
  - ユーザーが正常にログイン認証済み
  - Cloudflare Stream APIが利用可能
- **テスト手順**:
  1. POST /api/v1/media/stream/direct-upload に以下のリクエストを送信
     ```json
     {
       "fileName": "test_video.mp4",
       "fileSize": 52428800
     }
     ```
- **期待結果**:
  - HTTPステータス: 200 OK
  - レスポンス形式:
    ```json
    {
      "media_id": "uuid形式のメディアID",
      "upload_url": "Cloudflare Stream直接アップロードURL",
      "stream_uid": "Cloudflare StreamのUID",
      "expires_at": "1時間後のISO 8601形式日時"
    }
    ```
  - media_filesテーブルにstatus='uploading'、storage_provider='cloudflare_stream'でレコード作成される
- **優先度**: Critical
- **テストタイプ**: 統合テスト

##### TC-VID-002: 正常な動画アップロードURL取得（QuickTime）

- **テストID**: TC-VID-002
- **テスト名**: 正常な動画アップロードURL取得（QuickTime）
- **目的**: 有効なQuickTime動画ファイルに対してアップロードURLが正常に生成されることを確認する
- **前提条件**: ユーザーが正常にログイン認証済み
- **テスト手順**:
  1. POST /api/v1/media/stream/direct-upload に以下のリクエストを送信
     ```json
     {
       "fileName": "test_video.mov",
       "fileSize": 41943040
     }
     ```
- **期待結果**: HTTPステータス200 OK、有効なStream URLとmedia_idが返される
- **優先度**: High
- **テストタイプ**: 機能テスト

#### 2.2 異常系テスト

##### TC-VID-003: 動画ファイルサイズ超過

- **テストID**: TC-VID-003
- **テスト名**: 動画ファイルサイズ超過エラー
- **目的**: 100MBを超える動画ファイルサイズでリクエストした際のエラー処理を確認する
- **前提条件**: ユーザーが正常にログイン認証済み
- **テスト手順**:
  1. POST /api/v1/media/stream/direct-upload に以下のリクエストを送信
     ```json
     {
       "fileName": "large_video.mp4",
       "fileSize": 110100480
     }
     ```
- **期待結果**:
  - HTTPステータス: 413 Payload Too Large
  - エラーレスポンス:
    ```json
    {
      "error": {
        "code": "FILE_TOO_LARGE",
        "message": "Video size must be less than 100MB"
      }
    }
    ```
- **優先度**: High
- **テストタイプ**: エラーハンドリング

##### TC-VID-004: Cloudflare Stream API エラー

- **テストID**: TC-VID-004
- **テスト名**: Cloudflare Stream API エラー処理
- **目的**: Cloudflare Stream APIが利用不可の場合のエラー処理を確認する
- **前提条件**:
  - ユーザーが正常にログイン認証済み
  - Cloudflare Stream APIがエラー状態またはAPIキーが無効
- **テスト手順**:
  1. 正常なリクエストを送信
- **期待結果**:
  - HTTPステータス: 500 Internal Server Error
  - エラーレスポンス:
    ```json
    {
      "error": {
        "code": "STREAM_UPLOAD_FAILED",
        "message": "Failed to initialize video upload"
      }
    }
    ```
- **優先度**: High
- **テストタイプ**: エラーハンドリング

### 3. Cloudflare Stream Webhook処理

#### 3.1 正常系テスト

##### TC-WHK-001: アップロード完了イベント処理

- **テストID**: TC-WHK-001
- **テスト名**: アップロード完了イベント正常処理
- **目的**: Cloudflare Streamからのアップロード完了イベントが正常に処理されることを確認する
- **前提条件**:
  - 有効なWebhook署名
  - media_filesテーブルにstatus='uploading'のレコードが存在
- **テスト手順**:
  1. 有効な署名付きでPOST /api/webhooks/cloudflare-streamに以下を送信
     ```json
     {
       "id": "event_12345",
       "eventType": "video.upload.complete",
       "uid": "stream_uid_12345",
       "duration": 120,
       "dimensions": { "width": 1920, "height": 1080 }
     }
     ```
- **期待結果**:
  - HTTPステータス: 200 OK
  - media_filesテーブルのstatusが'ready'に更新される
  - processed_atとmetadataが適切に設定される
  - webhook_eventsテーブルにイベントが記録される
- **優先度**: Critical
- **テストタイプ**: 統合テスト

##### TC-WHK-002: アップロード失敗イベント処理

- **テストID**: TC-WHK-002
- **テスト名**: アップロード失敗イベント処理
- **目的**: Cloudflare Streamからのアップロード失敗イベントが正常に処理されることを確認する
- **前提条件**: 有効なWebhook署名
- **テスト手順**:
  1. 有効な署名付きでアップロード失敗イベントを送信
     ```json
     {
       "id": "event_12346",
       "eventType": "video.upload.failed",
       "uid": "stream_uid_12345",
       "errorMessage": "File format not supported"
     }
     ```
- **期待結果**:
  - HTTPステータス: 200 OK
  - media_filesテーブルのstatusが'failed'に更新される
  - error_messageが適切に設定される
- **優先度**: High
- **テストタイプ**: 統合テスト

##### TC-WHK-003: 重複イベント処理（冪等性）

- **テストID**: TC-WHK-003
- **テスト名**: 重複イベントの冪等性確認
- **目的**: 同一のイベントIDで複数回Webhookが送信された場合の冪等性を確認する
- **前提条件**: webhook_eventsテーブルに既に処理済みのイベントが存在
- **テスト手順**:
  1. 既に処理済みのイベントIDで再度Webhookを送信
- **期待結果**:
  - HTTPステータス: 200 OK
  - レスポンス: "Event already processed"
  - 重複処理されない
- **優先度**: Critical
- **テストタイプ**: 機能テスト

#### 3.2 異常系テスト

##### TC-WHK-004: 無効なWebhook署名

- **テストID**: TC-WHK-004
- **テスト名**: 無効なWebhook署名拒否
- **目的**: 不正な署名を持つWebhookが適切に拒否されることを確認する
- **前提条件**: 無効または不在のcf-signatureヘッダー
- **テスト手順**:
  1. 無効な署名でWebhookイベントを送信
- **期待結果**:
  - HTTPステータス: 401 Unauthorized
  - レスポンス: "Invalid signature"
- **優先度**: Critical
- **テストタイプ**: セキュリティテスト

##### TC-WHK-005: Webhook処理失敗時のDLQ送信

- **テストID**: TC-WHK-005
- **テスト名**: Webhook処理失敗時のDLQ送信
- **目的**: Webhook処理でエラーが発生した際にDLQに正常に送信されることを確認する
- **前提条件**:
  - 有効なWebhook署名
  - processStreamEvent()でエラーが発生する状況
- **テスト手順**:
  1. データベースエラーが発生するような状況でWebhookを送信
- **期待結果**:
  - HTTPステータス: 500 Internal Server Error
  - failed_webhook_eventsテーブルにイベントとエラー情報が記録される
- **優先度**: High
- **テストタイプ**: エラーハンドリング

### 4. メディア処理パイプライン

#### 4.1 画像処理テスト

##### TC-PROC-001: EXIF情報除去処理

- **テストID**: TC-PROC-001
- **テスト名**: EXIF情報除去処理確認
- **目的**: アップロードされた画像からEXIF情報が完全に除去されることを確認する
- **前提条件**: EXIF情報を含むJPEG画像がStorageにアップロード済み
- **テスト手順**:
  1. ImageProcessor.processUploadedImage()を実行
  2. 処理後の画像を取得してEXIF情報をチェック
- **期待結果**:
  - 画像からEXIF情報が完全に除去されている
  - 位置情報、カメラ情報等のメタデータが存在しない
  - 画像の向きは適切に調整されている
- **優先度**: Critical
- **テストタイプ**: セキュリティテスト

##### TC-PROC-002: 画像最適化処理

- **テストID**: TC-PROC-002
- **テスト名**: 画像最適化処理確認
- **目的**: アップロードされた画像が適切に最適化されることを確認する
- **前提条件**: 未処理の画像がStorageに存在
- **テスト手順**:
  1. ImageProcessor.processUploadedImage()を実行
  2. 処理前後のファイルサイズと品質を比較
- **期待結果**:
  - JPEGに統一変換される
  - 品質85%、プログレッシブJPEGで出力される
  - ファイルサイズが適切に最適化されている
  - Cache-Control: 31536000 (1年)が設定される
- **優先度**: High
- **テストタイプ**: 機能テスト

##### TC-PROC-003: サムネイル生成処理

- **テストID**: TC-PROC-003
- **テスト名**: 複数サイズサムネイル生成
- **目的**: 元画像から複数サイズのサムネイルが正常に生成されることを確認する
- **前提条件**: 処理対象の画像がStorageに存在
- **テスト手順**:
  1. ImageProcessor.processUploadedImage()を実行
  2. 生成された各サイズのサムネイルを確認
- **期待結果**:
  - 150px、300px、600pxの3つのサムネイルが生成される
  - 各サムネイルが正しい縦横比とサイズで生成される
  - fit: 'cover', position: 'center'で適切にクロップされる
  - JPEG品質80%で出力される
- **優先度**: High
- **テストタイプ**: 機能テスト

#### 4.2 エラー処理テスト

##### TC-PROC-004: 画像処理エラー時の処理

- **テストID**: TC-PROC-004
- **テスト名**: 画像処理エラー時の適切な例外処理
- **目的**: 破損画像や不正ファイルでの処理エラーが適切に処理されることを確認する
- **前提条件**: 破損または不正な画像ファイル
- **テスト手順**:
  1. 破損画像に対してImageProcessor.processUploadedImage()を実行
- **期待結果**:
  - 適切な例外がスローされる
  - エラーログが出力される
  - media_filesのstatusが'failed'に更新される
- **優先度**: High
- **テストタイプ**: エラーハンドリング

### 5. セキュリティテスト

#### 5.1 MIMEスニッフィング対策テスト

##### TC-SEC-001: 画像解凍爆弾攻撃対策

- **テストID**: TC-SEC-001
- **テスト名**: 画像解凍爆弾攻撃対策
- **目的**: 悪意のある画像ファイル（解凍爆弾）による攻撃が防御されることを確認する
- **前提条件**: 小さなファイルサイズだが解凍後に極大サイズになる画像ファイル
- **テスト手順**:
  1. 解凍爆弾画像でアップロードURL取得を試行
  2. 実際のアップロードと処理を実行
- **期待結果**:
  - ファイルサイズ制限により事前に拒否される、または
  - 画像処理時にメモリ制限により適切にエラー処理される
  - システムリソースが枯渇しない
- **優先度**: Critical
- **テストタイプ**: セキュリティテスト

##### TC-SEC-002: ファイル名サニタイゼーション

- **テストID**: TC-SEC-002
- **テスト名**: ファイル名サニタイゼーション処理
- **目的**: 危険な文字を含むファイル名が適切にサニタイズされることを確認する
- **前提条件**: パストラバーサルやスクリプト注入を狙った不正ファイル名
- **テスト手順**:
  1. 不正な文字を含むファイル名でアップロードURL取得を試行
     ```json
     {
       "fileName": "../../../etc/passwd<script>alert('xss')</script>",
       "mimeType": "image/jpeg",
       "fileSize": 1048576
     }
     ```
- **期待結果**:
  - ファイル名から危険な文字が除去される
  - 英数字、ピリオド、ハイフンのみ許可される
  - パストラバーサル攻撃が防御される
- **優先度**: Critical
- **テストタイプ**: セキュリティテスト

##### TC-SEC-003: 実ファイルMIME検証

- **テストID**: TC-SEC-003
- **テスト名**: アップロード後の実ファイルMIME検証
- **目的**: アップロード完了後に実際のファイル内容とMIMEタイプの整合性が検証されることを確認する
- **前提条件**: JPEGと申告しているが実際はPNGファイル
- **テスト手順**:
  1. image/jpegと申告してPNGファイルをアップロード
  2. 処理パイプラインでの検証を確認
- **期待結果**:
  - Sharp.jsによる実ファイル検証でMIME不整合が検出される
  - 不整合ファイルは処理が拒否される、またはエラー状態になる
- **優先度**: High
- **テストタイプ**: セキュリティテスト

### 6. パフォーマンステスト

#### 6.1 負荷テスト

##### TC-PERF-001: 同時アップロードURL生成負荷テスト

- **テストID**: TC-PERF-001
- **テスト名**: 同時アップロードURL生成負荷テスト
- **目的**: 複数ユーザーが同時にアップロードURL生成を要求した際のパフォーマンスを確認する
- **前提条件**: 100人の認証済みユーザー
- **テスト手順**:
  1. 100人のユーザーが同時にアップロードURL生成を要求
  2. レスポンス時間と成功率を測定
- **期待結果**:
  - 95%のリクエストが2秒以内に完了
  - エラー率が5%未満
  - データベース接続プールが枯渇しない
- **優先度**: Medium
- **テストタイプ**: パフォーマンステスト

##### TC-PERF-002: 画像処理パフォーマンステスト

- **テストID**: TC-PERF-002
- **テスト名**: 画像処理パフォーマンステスト
- **目的**: 画像処理パイプラインのパフォーマンスが許容範囲内であることを確認する
- **前提条件**: 10MB前後の高解像度JPEG画像
- **テスト手順**:
  1. 高解像度画像に対してprocessUploadedImage()を実行
  2. 処理時間とメモリ使用量を測定
- **期待結果**:
  - 10MB画像の処理が30秒以内に完了
  - メモリ使用量が500MB未満
  - CPUリソースが適切に開放される
- **優先度**: Medium
- **テストタイプ**: パフォーマンステスト

#### 6.2 リソース管理テスト

##### TC-PERF-003: メモリリーク検出テスト

- **テストID**: TC-PERF-003
- **テスト名**: 画像処理後のメモリリーク検出
- **目的**: 連続した画像処理でメモリリークが発生しないことを確認する
- **前提条件**: 複数の画像ファイル
- **テスト手順**:
  1. 100枚の画像を連続して処理
  2. 処理前後のメモリ使用量を監視
- **期待結果**:
  - メモリ使用量が処理前レベルまで適切に戻る
  - Sharp.jsのバッファが適切に解放される
- **優先度**: Medium
- **テストタイプ**: パフォーマンステスト

### 7. エラーハンドリング・復旧テスト

#### 7.1 外部サービス障害テスト

##### TC-ERR-001: Supabase Storage障害時の処理

- **テストID**: TC-ERR-001
- **テスト名**: Supabase Storage障害時の適切なエラー処理
- **目的**: Storage APIが利用不可の際の適切なエラー処理を確認する
- **前提条件**: Supabase Storageが一時的に利用不可
- **テスト手順**:
  1. Storage障害中にアップロードURL生成を試行
- **期待結果**:
  - HTTPステータス: 502 Bad Gateway
  - 適切なエラーメッセージが返される
  - 部分的なデータが残らない
- **優先度**: High
- **テストタイプ**: 障害テスト

##### TC-ERR-002: データベース障害時の処理

- **テストID**: TC-ERR-002
- **テスト名**: データベース障害時の適切なエラー処理
- **目的**: データベース接続エラー時の適切な処理を確認する
- **前提条件**: データベース接続が一時的に不可
- **テスト手順**:
  1. DB障害中にmedia_filesレコード作成を試行
- **期待結果**:
  - HTTPステータス: 500 Internal Server Error
  - 適切なエラーログが出力される
  - ロールバック処理が実行される
- **優先度**: High
- **テストタイプ**: 障害テスト

#### 7.2 リトライ機能テスト

##### TC-ERR-003: Webhook処理リトライテスト

- **テストID**: TC-ERR-003
- **テスト名**: Webhook処理失敗時のリトライ機能
- **目的**: Webhook処理で一時的エラーが発生した際のリトライ機能を確認する
- **前提条件**:
  - 一時的なデータベースエラーが発生する環境
  - DLQ機能が利用可能
- **テスト手順**:
  1. Webhook処理で一時的エラーを発生させる
  2. リトライ処理の実行を確認
- **期待結果**:
  - 指数バックオフでリトライが実行される
  - 最大5回のリトライ後、DLQに送信される
  - retry_countが適切にインクリメントされる
- **優先度**: High
- **テストタイプ**: 機能テスト

### 8. 統合テスト・エンドツーエンドテスト

#### 8.1 完全フローテスト

##### TC-E2E-001: 画像アップロード完全フローテスト

- **テストID**: TC-E2E-001
- **テスト名**: 画像アップロード～処理完了までの完全フローテスト
- **目的**: 画像アップロードから処理完了まで全体のフローが正常に動作することを確認する
- **前提条件**:
  - 認証済みユーザー
  - 有効なJPEG画像ファイル（EXIF情報含む）
- **テスト手順**:
  1. POST /api/v1/media/upload-url でアップロードURL取得
  2. 署名付きURLに画像をアップロード
  3. 画像処理パイプラインの完了を待機
  4. 最終的な状態を確認
- **期待結果**:
  - media_filesのstatusが'uploading'→'processing'→'ready'に遷移
  - EXIF情報が完全に除去されている
  - 最適化された画像とサムネイル（3サイズ）が生成されている
  - 全て処理が30秒以内に完了する
- **優先度**: Critical
- **テストタイプ**: エンドツーエンドテスト

##### TC-E2E-002: 動画アップロード完全フローテスト

- **テストID**: TC-E2E-002
- **テスト名**: 動画アップロード～処理完了までの完全フローテスト
- **目的**: 動画アップロードからCloudflare Stream処理完了まで全体のフローが正常に動作することを確認する
- **前提条件**:
  - 認証済みユーザー
  - 有効なMP4動画ファイル
- **テスト手順**:
  1. POST /api/v1/media/stream/direct-upload でアップロードURL取得
  2. Cloudflare Stream URLに動画をアップロード
  3. Webhookイベント受信と処理を確認
- **期待結果**:
  - media_filesのstatusが'uploading'→'ready'に遷移
  - stream_uidが適切に設定される
  - 動画のメタデータ（duration、dimensions）が記録される
  - Webhook経由で正常に処理される
- **優先度**: Critical
- **テストタイプ**: エンドツーエンドテスト

#### 8.2 混在データテスト

##### TC-E2E-003: 画像・動画混在アップロードテスト

- **テストID**: TC-E2E-003
- **テスト名**: 画像と動画を混在させた複数ファイルアップロードテスト
- **目的**: 1つの投稿に画像と動画が混在する場合の処理を確認する
- **前提条件**: 認証済みユーザー、複数のメディアファイル
- **テスト手順**:
  1. 画像ファイル2個、動画ファイル1個のアップロードURL取得
  2. 全てのファイルをアップロード
  3. 全体の処理完了を確認
- **期待結果**:
  - 全てのmedia_filesが正常に'ready'状態になる
  - 画像処理とStream処理が並行して正常完了する
  - データの整合性が保たれる
- **優先度**: High
- **テストタイプ**: 統合テスト
